<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
            font-family: serif;
            margin-left: 100px;
            margin-right: 100px;
            color: #fff;
        }
        .flex{
            display: flex;
            height: 97vh;
            justify-content: center;
            flex-direction: column;
        }
        .flexRow{
            display: flex;
            flex-direction: row;
        }
        .wallpaper {
            background-position: center;
            background-size: cover;
            width: 100%;
            height: 100vh;
            background-image: url('./imgs/izuddin-helmi-adnan-UQpTP-KqYRk-unsplash.jpg');
            position: absolute;
            right: 0px;
            top: 0px;
            z-index: -1;
            filter: brightness(0.30) opacity(1);

        }
        button{
            padding: 10px 12px 10px 12px;
            border: none;
            color: white;
            background-color:#145FA4;
            border-radius: 10px;
            margin-left:8px;
        }
        input{
            padding: 10px;
            border-radius: 10px;
        }
        p {
            font-size: 24px;
            margin: 0px;
            padding: 0px;
            padding-bottom: 5px;
        }
        .title {
            font-weight: bold;
            font-size: 24px;
            margin: 5px 5px 5px 8px;
        }
        .subtitle {
            font-size: 14px;
            margin: 5px 5px 5px 8px;
        }
        .Overflow {
            display: flex;
            overflow-x: auto;
            width: 67vw;
            margin-top: 40px;
        }
        .align{
            align-self: center;
            margin-right: 10px;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="flex">
        <div class="flexRow">
            <div class="wallpaper"></div>
            <img src="./imgs/nba (1).png" style="height: 40vh; width:20vw; position: relative; left: 0px;"/>
            <div>
            <div class="flexRow">
            <h1>Procure por times da NBA</h1>
            <form style="display: flex; margin-top: 10px; margin-left: 20px" action="/detalhar" method="POST">
                <input type="text" style="height: 30px;width: 300px;" name="postId" placeholder="Pesquisar">
            </form>
            <p style="display: flex; margin-top: 18px; margin-left: 20px">Você pesquisou por "<%=team %>"</p>
            </div>
            <div class="Overflow">
            <% for(let i = 0; i < teams.length; i++){ %>
                <div class="align">
                    <img src="<%= teams[i].image %>" style="width:150px;height:150px">
                    <p class="subtitle">Nome: <%= teams[i].full_name %></p>
                    <p class="subtitle">Cidade: <%= teams[i].city %></p>
                    <p class="subtitle">Conferencia: <%= teams[i].conference %></p>
                </div>
            <% } %>
            </div>
        </div>
        </div>
    </div>
</body>
</html>