<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
            font-family: serif;
            margin-left: 100px;
            margin-right: 100px;
            margin-top: 100px;
            color: #fff;
        }
        .flex{
            display: flex;
            height: 97vh;
            justify-content: flex-start;
            flex-direction: column;
        }
        .flexRow{
            display: flex;
            flex-direction: row;
        }
        .wallpaper {
            background-position: center;
            background-size: cover;
            width: 100%;
            height: 100vh;
            background-image: url('./imgs/izuddin-helmi-adnan-UQpTP-KqYRk-unsplash.jpg');
            position: absolute;
            right: 0px;
            top: 0px;
            z-index: -1;
            filter: brightness(0.30) opacity(1);

        }
        button{
            padding: 10px 12px 10px 12px;
            border: none;
            color: white;
            background-color:#145FA4;
            border-radius: 10px;
            margin-right: 30px;
        }
        input{
            padding: 10px;
            border-radius: 10px;
        }
        p {
            font-size: 24px;
            margin: 0px;
            padding: 0px;
            padding-bottom: 5px;
        }
        .containerblog {
            border-radius: 20px;
            background-color: #CB8A03;
            height: 35vh;
            width: 17vw;
            margin-right: 20px;
        }
        .title {
            font-weight: bold;
            font-size: 24px;
            margin: 5px 0px 5px 0px;
        }
        .subtitle {
            font-size: 14px;
        }
        .blogOverflow {
            display: flex;
            overflow-x: auto;
        }
        .Overflow {
            display: flex;
            overflow: auto;
            overflow-x: scroll;
            width: 27%;
            margin-top: 40px;
        }
        .blogImage{
            height: 120px;
            width: 100%;
            border-radius: 20px 20px 0px 0px;
        }
        .align{
            align-self: center;
            margin-right: 10px;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="flex">
        <div class="flexRow">
            <div class="wallpaper"></div>
            <img src="./imgs/nba (1).png" style="height: 40vh; width:20vw; position: relative; left: 0px;"/>
            <div>
            <div class="flexRow">
            <h1>Procure por times da NBA</h1>
            <form style="display: flex; margin-top: 10px; margin-left: 20px" action="/detalhar" method="POST">
                <input type="text" style="height: 30px;width: 300px;" name="postId" placeholder="Pesquisar">
            </form>
            </div>
            <div class="Overflow">
            <% for(let i = 0; i < teams.length; i++){ %>
                <div class="align">
                    <img src="<%= teams[i].image %>" style="width:150px;height:150px">
                    <p class="subtitle"><%= teams[i].full_name %></p>
                </div>
            <% } %>
            </div>
        </div>
        </div>

        <div style="margin-left: 100px;">
        <h1>Conheça nosso blog</h1>
        <div class="blogOverflow" >
        <% for(let i = 0; i < posts.length; i++){ %>
        <div class="containerblog">
            <img class="blogImage" src="./imgs/kobe.jpg"/>
            <p class="title"><%= posts[i].title.substr(0,30) %></p>
            <p class="subtitle"><%= posts[i].body.substr(0,150) %></p>
            <button class="buttonBlue">Ler mais</button>
        </div>
        <% } %>
    </div>
    </div>
    </div>
</body>
</html>